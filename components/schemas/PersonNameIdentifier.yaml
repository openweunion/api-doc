type: object
properties:
  primaryIdentifier:
    description: "Primary identifier for the person name."
    type: string
    example: "Smith"
  secondaryIdentifier:
    description: "Secondary identifier for the person name."
    type: string
    example: "John"
xml:
  name: PersonNameIdentifier
