post:
  tags:
    - Offramp Transaction Endpoints
  operationId: acceptTransaction
  summary: Accept Transaction
  description: |
    Accept a transaction by the Beneficiary VASP.

    **Preconditions:**
    - Transaction status must be: `APPROVED`
    - Only the Beneficiary VASP has permission to accept the transaction

    **Result:**
    - Transaction status changes to `ACCEPTED`
    - The Originator VASP will receive a webhook notification
    - The Originator VASP can then proceed to initiate the on-chain transfer

    **Flow:** This is called by the Beneficiary VASP after reviewing and accepting the transaction request from the Originator VASP.
  parameters:
    - $ref: ../components/parameters/PayloadHeader.yaml
    - $ref: ../components/parameters/SignatureHeader.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required:
            - id
          properties:
            id:
              type: string
              $ref: ../components/schemas/TransactionId.yaml
  responses:
    "200":
      description: Transaction accepted successfully
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../components/schemas/Response.yaml
              - type: object
                properties:
                  data:
                    oneOf:
                      - allOf:
                          - $ref: ../components/schemas/Transaction.yaml
                          - example:
                              status: "ACCEPTED"
    "400":
      description: Bad Request
      $ref: ../components/responses/Problem-400.yaml
    "401":
      description: Unauthorized
      $ref: ../components/responses/Problem-401.yaml
    "403":
      description: Forbidden - Only Beneficiary VASP can accept the transaction
      $ref: ../components/responses/Problem-403.yaml
    "404":
      description: Not Found - Transaction does not exist
      $ref: ../components/responses/Problem-404.yaml
    "409":
      description: Conflict - Transaction status is not APPROVED
      $ref: ../components/responses/Problem-409.yaml
    "500":
      description: Internal Server Error
      $ref: ../components/responses/Problem-500.yaml
